const config = {
    'development': {
        db: 'feira_dev',
        user: 'postgres', //Use o user do seu BD
        password: 'admin', //Use a senha do seu BD
        host: 'localhost',
        dialect: 'postgres'
    }
};

module.exports = config;
